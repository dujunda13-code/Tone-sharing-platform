# 音色创作与共享平台项目需求文档

| 文档属性 | 内容 |
| --- | --- |
| 文档类型 | 产品需求文档（PRD） |
| 文档版本 | V2.0（当前实现对齐版） |
| 编制日期 | 2026-09-22 |
| 产品形态 | Windows 本地部署的音色创作客户端 |
| 部署范围 | 单机、单 GPU、本地多账号 |
| 当前验收状态 | `INCOMPLETE`：自动化验证已通过，最终真实 GPU 全链路验收尚未完成 |
| 需求基线 | 当前仓库代码、`PROJECT_IMPLEMENTATION_PLAN.md`、`README.md`、`docs/implementation-log.md` |

> 本文档描述当前仓库已经实现或已经接通的产品功能，并将“代码已实现”“外部 API 已直接验证”“完整产品链路已最终验收”明确区分。若本文与历史 PRD 或早期训练型方案冲突，以当前项目约束和实际代码为准。

## 1. 项目概述

### 1.1 项目背景

本项目面向拥有合法声音授权的用户，提供参考音频审核、零样本音色建档、语音创作、音色共享及生成音频安全复检能力。系统以本地数据处理为主，在单台 Windows NVIDIA GPU 主机上运行。

产品不为普通用户训练专属模型权重。用户上传短参考音频并完成审核后，系统直接建立零样本音色档案。语音创作分为以下两种互斥模式：

1. **正常合成**：在本机使用冻结的 GPT-SoVITS V2ProPlus 基础模型生成语音。
2. **情绪合成**：用户另行确认云端处理后，调用 ModelVerse IndexTTS-2 生成带情绪语音。

两种模式的结果都必须回到本地，经过 CAM++ 相似度计算、AudioSeal 数字水印嵌入与复检、高频频谱指纹分析后才能成为可下载结果。

### 1.2 产品定位

本产品是一套面向个人授权音色创作、本地演示和研究验证的音色创作客户端，主要解决以下问题：

- 用户无需自行训练模型，即可用短参考音频建立可复用音色；
- 用户可以在本地生成普通语音，或在明确授权后生成云端情绪语音；
- 生成过程具备授权确认、敏感词拦截、来源水印和音色相似度记录；
- 本地账号可以管理音色、任务、作品、个人资料和音色广场互动；
- 管理员可以在本机管理账号状态和查看脱敏审计记录。

### 1.3 项目目标

1. 用户使用一段经过授权和审核的短参考音频创建 `ready` 零样本音色档案。
2. 用户使用音色档案完成中文或英文的本地语音合成。
3. 用户在单独确认云端处理后，使用 IndexTTS-2 完成多语言情绪语音合成。
4. 所有成功作品均通过本地敏感词、安全水印和频谱指纹链路。
5. 用户可以在本地音色广场发布、试听、下载、导入和互动。
6. 系统在单 GPU 环境串行执行计算任务，避免并发争抢显存。
7. 系统对模型缺失、哈希错误、GPU 不可用、授权缺失或云端失败采取 fail-closed，不发布未经验证的音频。

### 1.4 非目标

当前版本不包含以下能力：

- 为客户端用户训练 GPT、SoVITS 或解耦适配器权重；
- 公网 SaaS、多租户云平台、支付、计费或商业音色交易；
- 多机、多 GPU、Redis、Celery、Kubernetes 或通用云对象存储；
- 实时通话、直播变声、移动端应用；
- 对外开放的开发者 API 或第三方登录；
- 在云端处理用户账号、任务数据库和普通本地合成数据；
- 自动证明上传者必然拥有声音版权或人格权；
- 保证数字水印能够抵抗所有破坏性编辑。

仓库中的 `offline/emotion_adapter` 属于独立研究实验，不是当前客户端交付链路。实验候选尚未达到发布门槛，不能作为客户端已交付能力宣传。

## 2. 术语定义

| 术语 | 定义 |
| --- | --- |
| 参考音频 | 用户上传并确认拥有使用授权的真人语音文件。 |
| 有效人声 | 经过本地语音活动检测后确认包含人声的时长。 |
| 零样本音色档案 | 保存参考音频、确认文本、语言、情绪和基础模型 ID 的可复用档案，不包含用户专属训练权重。 |
| 正常合成 | 使用本机 GPT-SoVITS V2ProPlus 完成的语音合成。 |
| 情绪合成 | 在用户单独授权云端处理后，使用 ModelVerse IndexTTS-2 完成的语音合成。 |
| 本地安全门禁 | 敏感词检查、CAM++ 相似度、AudioSeal 水印和高频指纹等处理。 |
| 音色广场 | 同一套本地部署内用户发布、浏览和导入音色档案的共享区域。 |
| ready | 音色档案通过授权、审核、参考文件和基础模型检查，可以提交合成。 |
| fail-closed | 依赖缺失或检查失败时拒绝继续，不生成或发布降级结果。 |

## 3. 用户角色与权限

### 3.1 普通用户

普通用户可以：

- 注册、登录和退出本地账号；
- 查看自己的工作台和系统就绪状态；
- 上传已授权的参考音频；
- 查看并修改自动转写、语言和情绪识别结果；
- 创建和管理自己的零样本音色档案；
- 为同一音色添加多段已审核参考；
- 提交正常合成或情绪合成任务；
- 查看自己的任务、作品、安全指标和下载结果；
- 发布自己的可用音色到本地音色广场；
- 浏览、试听、点赞、收藏、评论、下载和导入广场音色；
- 编辑个人显示名称、简介和头像；
- 查看与自己有关的互动通知。

普通用户只能访问属于自己的数据，或音色广场中已经公开发布的数据。

### 3.2 管理员

管理员在普通用户能力之外可以：

- 查看本机用户列表和用户资源数量摘要；
- 启用或停用用户账号；
- 查看分页的脱敏审计事件；
- 查看系统健康状态和本机任务状态。

管理员不能：

- 查看用户密码明文；
- 绕过授权、敏感词、水印或安全检查；
- 将未经校验的模型设为可用；
- 通过管理界面读取用户未公开的原始音频内容；
- 把普通本地数据静默发送到外部服务。

## 4. 核心业务流程

### 4.1 登录与进入工作台

```text
注册或登录
→ 创建本地会话
→ 进入工作台
→ 查看系统就绪状态、资源数量和最近任务
```

### 4.2 零样本音色创建

```text
确认拥有声音授权
→ 上传 WAV/FLAC/MP3
→ 本地解码、质检和有效人声检测
→ 本地转写与自动情绪识别
→ 用户审核文本、语言和情绪
→ 确认审核结果
→ 创建 ready 零样本音色档案
```

该流程不创建训练 Job、训练特征、S1/S2 检查点或用户专属权重。

### 4.3 正常合成

```text
选择 ready 音色
→ 选择“正常合成”
→ 输入中文或英文文本
→ 设置切分、语速、停顿和采样参数
→ 确认合成授权
→ 本地敏感词检查
→ 串行 GPU Worker 生成 3 个候选
→ CAM++ 计算候选相似度并择优
→ AudioSeal 嵌入并复检水印
→ 高频指纹分析
→ 原子发布最佳结果
```

### 4.4 情绪合成

```text
选择 ready 音色
→ 选择“情绪合成”
→ 输入文本和情绪参数
→ 确认声音授权与云端处理授权
→ 本地敏感词检查
→ 校验参考音频是否满足云端约束
→ 调用 ModelVerse IndexTTS-2 生成 1 个候选
→ 下载到本地临时目录
→ CAM++、AudioSeal 和高频指纹处理
→ 通过后发布结果；失败则关闭流程且不回退本地模式
```

### 4.5 音色共享

```text
选择自己的 ready 音色
→ 填写音色简介并发布
→ 其他本地用户浏览、试听和互动
→ 用户确认导入授权
→ 导入为自己的零样本音色档案
→ 用导入音色进入语音创作
```

## 5. 功能需求

本章使用以下状态：

- **已实现**：代码、界面及自动化测试已存在；
- **已接通，待最终验收**：代码和自动化测试已存在，但仍需真实 GPU、真实授权参考或完整外部链路验收；
- **禁用**：系统必须明确拒绝，不得产生副作用。

### FR-001 本地账号注册与登录

**状态：已实现**

1. 系统必须提供注册、登录、当前会话查询和退出功能。
2. 用户使用用户名和密码注册，密码至少 8 位。
3. 密码必须以安全哈希保存，不得保存明文。
4. 会话必须通过本地 Cookie 管理，未登录用户不能访问工作台业务接口。
5. 被停用账号不得继续登录或访问受保护资源。
6. 管理员必须通过本机初始化流程创建，不提供普通公开管理员注册入口。

### FR-002 工作台与系统就绪状态

**状态：已实现**

1. 工作台必须显示数据集、音色、任务、活动任务和合成作品数量。
2. 工作台必须显示最近任务和可用音色。
3. 系统必须提供存活检查与就绪检查。
4. 就绪检查必须至少覆盖数据库、本地存储、GPU、GPT-SoVITS、Emotion2Vec 和 AudioSeal。
5. 关键依赖不可用时，界面必须显示明确的“需要检查”状态，不得伪装为系统可用。

### FR-003 参考音频上传与授权

**状态：已实现**

1. 支持上传 WAV、FLAC 和 MP3 文件。
2. 单文件最大 200 MB。
3. 用户必须在上传前确认拥有声音主体和用途授权。
4. 未确认授权时，系统必须拒绝上传，不得创建数据集记录或文件。
5. 文件必须保存在本地受控 `data/` 目录，路径不得逃逸存储根目录。
6. 系统必须使用内容 SHA-256 标识上传内容，不向前端暴露绝对路径。

### FR-004 本地音频质检与预处理

**状态：已实现**

1. 系统必须在本地完成音频解码、单声道规范化、有效人声检测和参考片段准备。
2. 客户端建档所用有效人声时长必须为 3–10 秒，包含边界。
3. 削波比例不得超过 1%；超出时必须阻止后续建档。
4. 20 dB SNR 为质量建议值，不是阻断门槛。低于该值可以继续，但必须显示质量警告。
5. 用户不需要自行切片，系统必须生成可审核片段。
6. 解码失败、无有效人声、时长不合格或参考文件缺失时必须停止流程并给出公开错误信息。

### FR-005 本地转写、情绪识别与人工审核

**状态：已实现**

1. 系统必须使用本地 FunASR 模型完成参考音频转写。
2. 系统必须使用本地 Emotion2Vec 完成自动情绪识别。
3. 自动情绪置信度低于 0.55 时必须映射为 `other`。
4. 本地审核情绪枚举为 `neutral`、`happy`、`sad`、`angry`、`fearful`、`disgusted`、`surprised`、`other`。
5. 用户可以修改转写文本、语言和情绪标签，但必须填写修改原因。
6. 系统必须同时保留自动结果和人工确认结果，不能用人工值覆盖原始自动结果。
7. 确认文本为空、审核未完成或有效人声不合格时不得创建音色档案。

### FR-006 零样本音色档案

**状态：已实现**

1. 审核通过后，系统必须直接创建 `ready` 零样本音色档案。
2. 档案必须记录所属用户、显示名称、数据集、基础模型 ID、参考片段、参考文本、语言、情绪、质量警告和创建时间。
3. 档案模式固定为 `zero_shot`。
4. 档案不得包含用户专属 S1/S2 权重目录。
5. 档案的可合成性必须同时取决于档案状态、参考文件存在和基础模型可用性。
6. 同一档案可以增加多段已经审核的参考音频，并记录每段参考的名称和情绪。
7. 用户可以删除非必要参考；系统必须防止删除后造成档案完全没有可用主参考。
8. 自动合成优先使用主参考，并可选取最多 3 段高质量辅助参考；人工情绪模式必须选择具有目标情绪标签的参考。

### FR-007 客户端训练入口关闭

**状态：禁用**

1. 客户端训练接口必须返回 `CLIENT_TRAINING_DISABLED`。
2. 被拒绝的训练请求不得创建 Job、训练特征、检查点、用户权重目录或 GPU 任务。
3. 前端不得向普通用户展示“训练模型”作为当前业务步骤。
4. 离线通用模型训练必须与客户端数据和运行目录隔离。

### FR-008 正常合成

**状态：已实现，待项目级最终真实验收**

1. 正常合成必须使用本地冻结 GPT-SoVITS V2ProPlus。
2. 本地模式支持中文 `zh` 和英文 `en`，文本最多 1000 字符。
3. 用户可以设置以下参数：
   - 文本切分：不切分、每四句、每 50 字、中文句号、英文句号、标点切分；
   - 语速：0.6–1.65；
   - 句间停顿：0.1–0.5 秒；
   - `top_k`：1–100；
   - `top_p`：0–1；
   - `temperature`：0–1。
4. 用户可以使用自动参考情绪，或选择档案中已经存在的人工情绪参考。
5. 每个正常合成任务必须串行生成 3 个候选，并以 CAM++ 相似度最高者作为最佳候选。
6. 三个候选必须逐个生成和检查，不得并发占用 GPU。
7. 任一候选只有通过水印嵌入与载荷复检后才能参与择优。
8. 本地基础模型或 GPU 不可用时必须失败，不得自动改用云端服务。

### FR-009 云端情绪合成

**状态：代码已接通，ModelVerse API 直接冒烟已通过，完整业务安全链路待最终验收**

1. 情绪合成必须调用固定模型 `IndexTeam/IndexTTS-2`。
2. 用户必须额外确认云端处理授权；普通合成授权不能替代云端授权。
3. 云端模式支持中文、英文、日文、西班牙文和阿拉伯文，文本最多 600 字符。
4. 云端参考必须为 WAV 或 MP3、5–30 秒、至少 16 kHz 且不超过 20 MiB。
5. 由于本地档案参考被限制为 3–10 秒，实际可用于云端模式的档案参考必须同时满足两套约束，即 5–10 秒。
6. 云端情绪控制必须支持：
   - 自动控制；
   - 标签控制；
   - 1–200 字符的情绪描述；
   - 八维单一或混合情绪向量。
7. 标签控制支持 `neutral`、`happy`、`angry`、`sad`、`afraid`、`disgusted`、`melancholic`、`surprised`、`calm`。
8. 八维向量每项范围为 0–1.5，总和必须大于 0 且不超过 1.5；单一模式必须只有一个非零项，混合模式至少两个非零项。
9. 云端参数必须支持：情绪强度 0–1、采样率 22050/44100/48000、语速 0.25–4、音量增益大于 0 且不超过 4、随机采样开关、句间静音 0–5000 ms。
10. 每个云端任务只生成 1 个候选，避免未确认的额外计费。
11. 云端输出必须先下载到本地临时目录，再进入与本地模式一致的安全链路。
12. 云端密钥只能从 `COMPSHARE_API_KEY` 环境变量读取，不得写入源码、SQLite、日志、命令行参数或前端。
13. 默认 API 地址为 `https://api.modelverse.cn/v1/audio/infer`；仅测试或私有网关场景允许通过 `MODELVERSE_API_BASE_URL` 覆盖。
14. API 未配置、鉴权失败、网络错误、返回内容无效或响应不是可读 WAV 时必须 fail-closed。
15. 云端失败不得静默回退为无情绪本地结果。

### FR-010 合成授权与敏感词拦截

**状态：已实现**

1. 所有合成请求必须明确确认声音和用途授权。
2. 系统必须在创建任务和生成临时音频前完成敏感词检查。
3. 敏感词归一化必须处理大小写、全半角、插入空格和常见连接符变体。
4. 被拦截请求返回公开错误码 `SENSITIVE_TEXT_BLOCKED`。
5. 日志不得保存完整敏感文本，只能保存必要的脱敏命中信息。

### FR-011 CAM++ 音色相似度

**状态：已实现，最终真实质量验收待完成**

1. 系统必须使用本地 CAM++ 计算生成音频与档案主参考的说话人相似度。
2. 当前发布目标为每条输出相似度 `>=0.90`。
3. 当前实现中，低于 0.90 的结果会记录 `SPEAKER_SIMILARITY_BELOW_RECOMMENDED` 质量警告；它不是单独的硬阻断条件。
4. 正常合成必须以候选相似度作为主要择优依据。
5. 相似度值和质量警告必须在任务、作品或安全信息中可查看。
6. 最终项目质量是否通过仍由不带 GPU 跳过项的真实验收报告决定。

### FR-012 AudioSeal 数字水印

**状态：已实现，攻击矩阵待最终真实验收**

1. 每个成功候选必须嵌入 AudioSeal 16-bit 水印。
2. 水印载荷由任务 ID 确定，同一任务必须能够复核载荷一致性。
3. 水印检测概率必须 `>=0.80`，且载荷必须与任务匹配。
4. 水印概率不足或载荷不匹配时，候选不得发布。
5. 如果所有候选都未通过水印检查，整个任务必须失败并返回 `WATERMARK_VERIFICATION_FAILED`。
6. 安全中心必须支持对已有合成任务重新检测水印。
7. 严重噪声、裁剪、压缩或重采样导致无法可靠检测时，只能显示“来源无法可靠确认”。

### FR-013 高频频谱指纹

**状态：已实现**

1. 系统必须分析 Nyquist 频率的 0.75–0.95 相对频带。
2. 指纹记录必须包含采样率、高频频带、频带能量比、谱平坦度、相对档案 z-score 和异常标志。
3. 指纹必须在输出发布前生成并随任务结果保存。
4. 指纹异常用于安全提示和审计，不得伪造为绝对来源证明。

### FR-014 合成任务与作品管理

**状态：已实现**

1. 合成请求必须进入 SQLite 持久任务队列。
2. GPU Worker 数量固定为 1，同一时刻只能有一个 GPU 任务运行。
3. 任务状态包括 `queued`、`running`、`succeeded`、`failed` 和 `canceled`。
4. 用户可以按状态筛选并查看自己的任务。
5. 任务必须显示创建时间、开始时间、结束时间、公开错误码和进度信息。
6. 进程重启后，遗留的运行中任务必须安全恢复或重新排队。
7. 只有成功发布的结果可以通过受保护接口试听或下载。
8. 合成临时文件必须在任务结束或失败后清理。

### FR-015 音色广场

**状态：已实现**

1. 用户可以把自己的 `ready` 音色发布到同一部署实例的音色广场。
2. 发布内容包含音色名称、参考数量、参考情绪、作者信息和不超过 500 字的简介。
3. 广场支持“全部”“我的发布”“我的收藏”筛选。
4. 广场支持按最新发布或点赞数量排序。
5. 用户可以查看音色详情、试听主参考、下载音色包和下载主参考音频。
6. 用户可以点赞、取消点赞、收藏、取消收藏、评论和查看评论。
7. 点赞、收藏和取消操作必须幂等，失败时界面必须回滚本地乐观状态。
8. 发布者可以删除自己的发布。
9. 用户导入他人音色前必须明确确认获得相应使用授权。
10. 导入后的音色必须成为当前用户拥有的零样本档案，并遵守相同的合成和安全规则。

### FR-016 个人资料与通知

**状态：已实现**

1. 用户可以修改 1–32 字符的显示名称和不超过 200 字符的个人简介。
2. 用户可以上传头像，并通过受控头像接口查看。
3. 个人中心必须显示发布数量和收到的点赞数量。
4. 个人中心必须提供“我的发布”“我的收藏”“我的评论”“通知历史”视图。
5. 系统必须对相关广场互动生成通知。
6. 用户可以查看未读数量，并按通知 ID 或全部标记已读。

### FR-017 管理中心与审计

**状态：已实现**

1. 管理中心仅允许 `admin` 角色访问。
2. 管理员可以查看用户 ID、用户名、角色、状态、创建时间以及数据集、音色和任务数量。
3. 管理员可以把普通用户状态修改为 `active` 或 `disabled`。
4. 管理员可以查看分页审计事件。
5. 审计事件不得包含密码、API 密钥、绝对本地路径、完整敏感文本或原始音频内容。

### FR-018 本地模型注册与运行时校验

**状态：已实现，依赖部署模型完整性**

1. 客户端基础模型必须随本地部署提供，不能在业务运行时按模型名联网下载。
2. 激活基础模型固定为 `gpt-sovits-v2proplus-official`，上游 tag 固定为 `20250606v2pro`。
3. GPT、SoVITS、BERT、HuBERT、FunASR、Emotion2Vec、CAM++ 和 AudioSeal 权重必须位于 `models/` 或登记的受控位置。
4. 所有激活权重必须登记在 `models/checksums.sha256`。
5. 应用启动和 Worker 接手任务时必须重新校验模型注册表和文件哈希。
6. 模型缺失或哈希不匹配时必须返回明确公开错误并停止任务。

## 6. 页面需求

| 页面 | 主要功能 | 访问角色 |
| --- | --- | --- |
| 登录页 | 用户名、密码、登录、跳转注册、错误提示 | 未登录用户 |
| 注册页 | 用户名、密码、确认密码、密码规则 | 未登录用户 |
| 工作台 | 数量统计、最近任务、可用音色、系统状态、快捷入口 | 已登录用户 |
| 音色广场 | 浏览、筛选、排序、试听、互动、下载、导入和发布 | 已登录用户 |
| 创建音色 | 授权确认、文件上传、预处理、审核、零样本建档、多参考添加 | 已登录用户 |
| 我的音色 | 音色列表、参考数量、情绪、质量警告、进入创作 | 已登录用户 |
| 语音创作 | 正常/情绪模式切换、文本、语言、参数、授权、提交 | 已登录用户 |
| 任务中心 | 状态筛选、任务进度、失败原因和时间信息 | 已登录用户 |
| 安全中心 | 合成记录、水印复检、相似度、指纹和来源状态 | 已登录用户 |
| 个人中心 | 资料、头像、发布、收藏、评论和通知 | 已登录用户 |
| 管理中心 | 用户状态、资源摘要和审计事件 | 管理员 |

页面必须使用明确状态描述，例如“待审核”“可用”“排队中”“执行中”“已失败”“水印复检失败”“来源无法可靠确认”，不得用笼统的“处理完成”掩盖失败或跳过项。

## 7. 数据需求

### 7.1 核心数据实体

| 实体 | 主要内容 |
| --- | --- |
| 用户 | 用户名、密码哈希、角色、状态、创建时间 |
| 会话 | 会话令牌摘要、用户、过期时间 |
| 数据集 | 所属用户、授权时间、状态、有效人声时长 |
| 音频资产 | 数据集、受控相对路径、时长、内容信息 |
| 审核片段 | 转写、语言、自动情绪、人工情绪、质量指标、审核时间 |
| 音色档案 | 所属用户、显示名称、零样本模式、基础模型 ID、状态 |
| 音色参考 | 资产、片段、提示文本、语言、情绪、主参考标志 |
| 任务 | 类型、状态、请求参数、进度、错误、开始和结束时间 |
| 合成记录 | 音色、语言、输出路径、水印、相似度、指纹和警告 |
| 广场发布 | 音色、作者、简介、互动计数、发布时间 |
| 广场互动 | 点赞、收藏、评论、通知 |
| 用户资料 | 显示名称、简介、头像 |
| 审计事件 | 事件类型、主体、脱敏元数据和时间 |

### 7.2 本地目录

```text
data/app.db       本地业务数据库
data/uploads/     用户上传的授权参考音频
data/datasets/    规范化参考片段和审核产物
data/features/    可再生分析特征
data/temp/        任务中间文件，任务结束后清理
data/outputs/     通过水印复检的作品
data/logs/        服务、Worker 和启动日志
models/           部署阶段下载并校验的基础模型
reports/          验收和质量报告
```

### 7.3 数据隔离

1. 普通用户资源必须通过 `owner_user_id` 隔离。
2. 前端请求不得提交任意本地文件路径。
3. 后端返回不得包含绝对本地路径。
4. 音色广场只暴露用户主动发布的数据。
5. 情绪合成只允许上传当前任务选中的授权参考和必要文本。

## 8. 外部接口需求

### 8.1 ModelVerse IndexTTS-2

| 项目 | 要求 |
| --- | --- |
| 接口 | `POST /v1/audio/infer` |
| 默认地址 | `https://api.modelverse.cn` |
| 模型 | `IndexTeam/IndexTTS-2` |
| 请求方式 | `multipart/form-data` |
| 凭据来源 | 进程环境变量 `COMPSHARE_API_KEY` |
| 输入 | 授权参考音频、文本和受支持的情绪/音频参数 |
| 输出 | 可解码 WAV |
| 失败策略 | fail-closed，不回退本地合成 |

### 8.2 隐私要求

1. 选择正常合成时不得调用 ModelVerse。
2. 选择情绪合成但未勾选云端授权时不得创建任务或发送请求。
3. API 密钥不得通过前端传输。
4. 云端错误信息必须净化，不得向前端返回签名 URL、请求头或服务内部响应体。
5. 下载的云端结果必须进入受控临时目录，并在本地安全处理完成后清理中间文件。

## 9. 非功能需求

### NFR-001 运行环境

- Windows 10/11；
- Python 3.10；
- PyTorch 2.5.1；
- CUDA 12.4；
- FunASR 1.4.12；
- AudioSeal 0.2.0；
- 单张 NVIDIA GPU，设备固定为 `cuda:0`；
- 运行时总显存硬门槛为 8000 MiB；
- Node.js 应满足 Vite 支持版本，建议 20.19+ 或 22.12+。

### NFR-002 性能与并发

1. GPU Worker 数量固定为 1。
2. GPU 阶段必须使用 batch size 1、FP16 和串行处理。
3. 正常合成的 3 个候选必须顺序执行。
4. 系统当前不承诺固定响应时间或吞吐 SLA；界面必须持续展示任务状态和进度。
5. 不得为了规避显存不足降低水印阈值、测试样本或安全规则。

### NFR-003 安全

1. 所有输入必须执行类型、长度、枚举和路径边界校验。
2. 子进程调用不得拼接用户输入为 shell 命令。
3. 密码、云端密钥和会话令牌不得写入普通日志。
4. 用户上传文件不得作为可执行文件处理。
5. 基础模型必须使用 SHA-256 校验，失败时停止运行。
6. 水印、授权和敏感词检查不得提供关闭开关。

### NFR-004 可靠性

1. SQLite 任务队列必须保证同一任务不被两个 Worker 同时认领。
2. 应用重启后必须恢复中断任务状态。
3. 输出必须先写临时文件，再通过原子发布进入 `data/outputs/`。
4. 失败任务不得留下可下载的半成品。
5. 云端超时、错误或无效 WAV 不得影响已存在的本地档案和作品。

### NFR-005 可维护性与测试

1. 业务改动必须遵循 Red → Green → Refactor。
2. 后端必须通过 Ruff、compileall、单元测试和集成测试。
3. 前端必须通过 Vitest、TypeScript 类型检查和 Vite 构建。
4. 外部 API 自动化测试只能使用模拟服务，不得自动消耗积分。
5. 最终真实验收必须使用部署者明确授权的参考音频。

### NFR-006 可审计性

1. 关键操作必须具有任务 ID、用户 ID、时间和公开结果。
2. 实施和验收命令、退出码、关键指标及阻塞原因必须记录在 `docs/implementation-log.md`。
3. 最终报告存在任何跳过项时不得标记为 `PASS`。

## 10. 公开错误要求

系统至少需要稳定表达以下错误场景：

| 错误码或场景 | 含义 |
| --- | --- |
| `CLIENT_TRAINING_DISABLED` | 客户端不允许训练用户权重 |
| `SENSITIVE_TEXT_BLOCKED` | 合成文本命中敏感词 |
| `VOICE_PROFILE_NOT_READY` | 音色不存在或不可合成 |
| `EMOTION_REFERENCE_UNAVAILABLE` | 档案没有目标情绪参考 |
| `BASE_MODEL_UNAVAILABLE` | 基础模型缺失、未登记或校验失败 |
| `CLOUD_TTS_NOT_CONFIGURED` | 云端情绪合成未配置可用凭据 |
| `WATERMARK_VERIFICATION_FAILED` | 水印概率或载荷未通过 |
| `SYNTHESIS_PIPELINE_FAILED` | 合成安全流水线发生未分类失败 |
| 参考时长不合格 | 有效人声不在本地 3–10 秒范围，或不满足云端 5–30 秒要求 |
| 云端请求失败 | API 鉴权、网络、状态码或响应音频错误 |

公开错误响应不得包含堆栈、绝对路径、密钥、完整敏感文本或外部签名 URL。

## 11. 验收标准

### 11.1 功能验收

| 编号 | 验收项 | 通过标准 |
| --- | --- | --- |
| AC-01 | 账号与权限 | 普通用户可注册登录；管理员页面拒绝普通用户；停用账号不可继续使用。 |
| AC-02 | 数据隔离 | 用户只能读取和修改自己的私有数据，广场发布内容除外。 |
| AC-03 | 上传授权 | 未确认授权时不创建数据集或文件。 |
| AC-04 | 参考质检 | WAV/FLAC/MP3 可处理；3–10 秒、削波和有效人声门禁生效；低 SNR 只告警。 |
| AC-05 | 审核 | 自动转写和情绪结果可见；人工修改原因被记录；未审核数据不能建档。 |
| AC-06 | 零样本建档 | 审核通过后档案直接为 `ready`，没有训练任务、检查点或个人权重。 |
| AC-07 | 多参考 | 可添加多段审核参考；人工情绪只能选择已存在的对应情绪参考。 |
| AC-08 | 正常合成 | 本地 V2ProPlus 使用真实 GPU 生成 3 个候选并按 CAM++ 择优。 |
| AC-09 | 情绪合成 | 明确云端授权后，IndexTTS-2 生成 1 个候选；失败不回退本地。 |
| AC-10 | 敏感词 | 被拦截文本不创建 Job 或临时音频。 |
| AC-11 | 水印 | 发布结果的 AudioSeal 概率不低于 0.80，且载荷与任务匹配。 |
| AC-12 | 指纹 | 发布结果保存完整高频指纹字段。 |
| AC-13 | 任务恢复 | 服务重启后任务状态可恢复，且不会并发运行两个 GPU 任务。 |
| AC-14 | 音色广场 | 可发布、浏览、试听、互动、下载和经授权导入音色。 |
| AC-15 | 个人与通知 | 可维护资料和头像，可查看互动通知并标记已读。 |
| AC-16 | 管理 | 管理员可查看用户摘要、启停用户并查看脱敏审计。 |

### 11.2 最终真实验收

最终项目验收必须满足：

1. 使用用户明确授权、原始时长和有效人声均为 3–10 秒的真实参考音频；
2. 不使用 `-SkipGpu` 执行 `scripts/verify.ps1`；
3. 本地基础模型、GPU、CAM++、AudioSeal 和指纹组件全部真实运行；
4. 完成上传、预处理、人工审核、零样本建档、正常合成、安全复检和下载；
5. 情绪模式使用满足 5–10 秒交集约束的授权 WAV/MP3，完成一次完整业务流水线验证；
6. 不存在占位 Worker、伪造指标、跳过安全步骤或预先生成的用户权重；
7. `reports/final-evaluation.json` 的全部必需检查为 `PASS`，且 `final_result` 为 `PASS`。

如果缺少参考、模型、GPU、云端凭据、质量指标、安全指标，或出现任何 skip，最终结果必须保持 `INCOMPLETE`。

## 12. 当前实现与验收状态

截至 2026-09-22：

| 项目 | 当前状态 |
| --- | --- |
| 本地账号、权限、工作台 | 已实现并有自动化测试 |
| 参考上传、质检、转写、情绪审核 | 已实现并有自动化测试 |
| 零样本建档、多参考管理 | 已实现并有自动化测试 |
| 本地 GPT-SoVITS 双语合成 | 已实现；项目级最终真实验收待完成 |
| ModelVerse IndexTTS-2 | 代码已接通；直接 API 冒烟成功；完整本地安全链路待最终验收 |
| CAM++、AudioSeal、高频指纹 | 已接入；最终真实攻击矩阵待完成 |
| 音色广场、互动、导入 | 已实现并有前后端测试 |
| 个人资料、头像、通知 | 已实现并有测试 |
| 管理中心与审计 | 已实现并有测试 |
| 后端自动化 | 最近记录为 351 项测试通过，另有需要真实条件的用例未计入 |
| 前端自动化 | 最近记录为 103 项测试通过，类型检查和生产构建通过 |
| 最终验收报告 | `INCOMPLETE`，GPU 和授权参考相关检查仍为跳过状态 |

直接云端 API 冒烟产生了可读 WAV，但该样本没有经过完整业务管线的 CAM++、AudioSeal 和高频指纹处理，因此不能视为正式可发布作品。

## 13. 已知限制与风险

| 风险或限制 | 当前处理方式 |
| --- | --- |
| 参考音频低质量 | SNR 低于 20 dB 提示风险；削波和有效人声不合格时阻断。 |
| 3–5 秒档案不能用于云端模式 | 本地模式可使用；云端模式要求增加一段满足 5–10 秒交集的参考。 |
| 云端服务不可用或计费变化 | 云端任务 fail-closed，不回退本地；每次只生成一个候选。 |
| 云端数据隐私 | 仅在独立授权后发送必要参考和文本；其余业务数据保持本地。 |
| 音色相似度低于目标 | 当前记录质量警告；最终质量以真实验收报告为准。 |
| 水印被破坏 | 输出“来源无法可靠确认”，不作绝对判断。 |
| 8GB 级显存不足 | 串行任务、FP16、batch size 1；不足时拒绝任务，不降低安全阈值。 |
| 本机数据丢失 | 用户需要自行备份重要参考、档案数据和作品。 |
| 音色授权真实性 | 产品记录用户确认，但无法替代法律层面的身份和授权核验。 |
| Node 版本偏低 | 当前环境 Node 22.2 可构建但低于 Vite 建议版本，应升级至 22.12+。 |

## 14. 完成定义

项目只有同时满足以下条件才能声明完成：

1. 本文档中的 P0 核心流程均存在真实实现，不使用占位组件；
2. 后端 lint、编译、单元测试和集成测试全部通过；
3. 前端测试、类型检查和生产构建全部通过；
4. 本地正常合成在真实 GPU 上完成最终端到端验收；
5. 云端情绪合成完成一次经过完整本地安全门禁的真实业务验收；
6. 水印和安全攻击矩阵达到项目门槛；
7. 不产生客户端训练权重或检查点副作用；
8. `reports/final-evaluation.json` 无跳过项并写入 `PASS`；
9. `docs/implementation-log.md` 记录全部关键命令、退出码、指标和外部副作用。

在上述条件全部满足前，项目状态必须如实表述为“主要功能已实现，最终验收未完成”。
